# Caminho de arquivo, não connection string — SQLite não usa servidor.
# Em produção (Railway), aponta pro path de um Volume persistente, ex.: /data/olimpiada.db
DATABASE_URL=./data/olimpiada.db

JWT_SECRET=troque_por_um_segredo_forte_aqui

PORT=3001
FRONTEND_URL=http://localhost:5173
